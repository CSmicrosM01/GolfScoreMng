# 松本杯 ゴルフスコア管理Webアプリ 設計・実装レポート

**作成日**: 2026年1月5日

---

## 目次

1. [プロジェクト概要](#1-プロジェクト概要)
2. [システム構成](#2-システム構成)
3. [機能設計](#3-機能設計)
4. [データ設計](#4-データ設計)
5. [画面設計](#5-画面設計)
6. [ルール・ロジック](#6-ルールロジック)
7. [年度別ファイル対応（2026/1/5実装）](#7-年度別ファイル対応202615実装)
8. [AWS構成と費用](#8-aws構成と費用)
9. [運用・保守](#9-運用保守)

---

## 1. プロジェクト概要

### 1.1 目的

年間のゴルフスコアを管理するWebアプリケーション。6名のメンバー（松本、正本、渡邉、近藤、比企、内藤）で運用する「松本杯」というゴルフコンペのスコア管理を行う。

### 1.2 要件

| 要件 | 詳細 |
|------|------|
| ホスティング | AWS S3 静的ウェブサイトホスティング |
| データベース | 不使用（JSONファイルで管理） |
| データ同期 | Lambda関数URL経由でS3上のJSONを共有 |
| 対応デバイス | PC、タブレット、スマートフォン |
| 外部ライブラリ | 不使用（Vanilla JavaScript） |

### 1.3 メンバー

| 名前 | 備考 |
|------|------|
| 松本 | - |
| 正本 | - |
| 渡邉 | - |
| 近藤 | - |
| 比企 | - |
| 内藤 | - |

---

## 2. システム構成

### 2.1 技術スタック

| 分類 | 技術 |
|------|------|
| フロントエンド | HTML5 + CSS3 + Vanilla JavaScript |
| ホスティング | AWS S3 静的ウェブサイトホスティング |
| API | AWS Lambda関数URL |
| データ保存 | localStorage（ローカル）+ S3（共有） |

### 2.2 アーキテクチャ図

```
┌─────────────┐     ┌─────────────────────┐     ┌─────────────┐
│   ブラウザ   │────>│  S3 静的ホスティング  │     │             │
│  (HTML/JS)  │<────│  index.html         │     │             │
└─────────────┘     │  styles.css         │     │             │
       │            │  app.js             │     │     S3      │
       │            └─────────────────────┘     │   バケット   │
       │                                        │             │
       │            ┌─────────────────────┐     │ config.json │
       └───────────>│   Lambda関数URL      │<───>│ data-2025   │
         GET/POST   │   (index.mjs)       │     │ data-2026   │
                    └─────────────────────┘     └─────────────┘
```

### 2.3 ファイル構成

```
GolfScoreMng/
├── index.html          # メインHTMLファイル
├── styles.css          # スタイルシート
├── app.js              # アプリケーションロジック
├── data.js             # 初期データ（オフライン用）
├── config.json         # 共通設定（ハンディキャップ等）
├── data-2025.json      # 2025年度スコアデータ
├── data-2026.json      # 2026年度スコアデータ
├── lambda/
│   └── index.mjs       # Lambda関数コード
├── README.md           # プロジェクト説明
├── DEPLOY.md           # S3デプロイ手順
├── AWS_SETUP.md        # Lambda設定手順
└── REPORT_20260105.md  # このレポート
```

---

## 3. 機能設計

### 3.1 機能一覧

| # | 機能名 | 説明 | 対象ユーザー |
|---|--------|------|-------------|
| 1 | ログイン | 6名から選択してログイン | 全員 |
| 2 | ダッシュボード | 各賞、個人成績、総合ランキング表示 | 全員 |
| 3 | ランキング | 総合順位、ベストスコア、パット平均 | 全員 |
| 4 | スコア一覧 | 全ラウンド表示、フィルタリング | 全員 |
| 5 | スコア入力 | 個人用（ログインユーザー専用） | 全員 |
| 6 | 一括入力 | 幹事用（全員のスコア入力・編集・削除） | 幹事 |
| 7 | 各賞・ルール | 開催条件、賞内容、ハンディキャップ設定 | 全員 |
| 8 | 操作方法 | 使い方説明 | 全員 |

### 3.2 機能詳細

#### 3.2.1 ログイン画面

- 6名のユーザーボタンから選択
- 年度選択（動的生成）
- 選択年度のランキング・ベストスコア・ベストパット表示

#### 3.2.2 ダッシュボード

| 表示項目 | 説明 |
|----------|------|
| カップ名 | 「2026〇〇杯」形式で表示 |
| 総開催回数 | 有効ラウンド数 |
| 有効参加者数 | 3回以上参加した人数 |
| ベストスコア賞 | 最低スコアと達成者 |
| パット平均ベスト賞 | 最低パット平均と達成者 |
| 個人成績 | ログインユーザーの参加回数、平均スコア、ベストスコア、パット平均 |
| 総合ランキング | ハンディ適用済みの順位表 |
| ホールインワン | 達成者リスト |
| イーグル | 達成者リスト |

#### 3.2.3 ランキング

| ランキング種別 | 説明 |
|---------------|------|
| 総合順位 | 平均スコア順（ハンディ適用切替可） |
| ベストスコア | 単発ベストスコアTOP10 |
| パット平均 | パット平均順 |

#### 3.2.4 スコア一覧

- 全ラウンドを表形式で表示
- ユーザー/コースでフィルタリング可能
- 参加人数3名未満は薄く表示（無効扱い）
- 各ラウンドに編集ボタン

#### 3.2.5 スコア入力（個人用）

- ログインユーザーが自分のスコアのみを入力
- 日付、コース、スコア、パット数を入力
- 同じ日付・コースのラウンドがあれば自動で追加

#### 3.2.6 一括入力（幹事用）

- 全員のスコアをまとめて入力
- 既存ラウンドの編集・削除
- ホールインワン/イーグル記録
- データエクスポート/インポート

#### 3.2.7 各賞・ルール

- 開催条件、参加条件の説明
- 各順位の賞内容
- ハンディキャップ設定
- カップ名設定

---

## 4. データ設計

### 4.1 データ構造（新形式：年度別ファイル）

#### config.json（共通設定）

```json
{
  "availableYears": [2025, 2026],
  "handicaps": {
    "松本": 0,
    "正本": 0,
    "渡邉": 0,
    "近藤": 0,
    "比企": 0,
    "内藤": 0
  }
}
```

#### data-YYYY.json（年度別データ）

```json
{
  "year": 2025,
  "rounds": [
    {
      "roundNumber": 1,
      "date": "2025-01-01",
      "course": "箱根くらかけ",
      "scores": {
        "松本": { "score": 88, "putt": 34 },
        "渡邉": { "score": 106, "putt": 39 },
        "正本": { "score": 102, "putt": 41 }
      }
    }
  ],
  "holeInOnes": [
    {
      "user": "松本",
      "date": "2025-05-01",
      "course": "サカワ",
      "hole": 7
    }
  ],
  "eagles": [],
  "cupName": "松本杯"
}
```

### 4.2 ローカルストレージ

| キー | 内容 |
|------|------|
| `golfScoreApp` | 年度データ（旧形式互換） |
| `golfScoreAppConfig` | 共通設定 |

### 4.3 データ同期フロー

```
[保存時]
ブラウザ → localStorage保存 → Lambda POST → S3保存

[読込時]
ブラウザ → Lambda GET → S3取得 → localStorage保存
```

---

## 5. 画面設計

### 5.1 画面遷移図

```
┌──────────────┐
│  ログイン画面  │
│  (年度選択)   │
└──────┬───────┘
       │ ユーザー選択
       ▼
┌──────────────┐
│  メイン画面   │
│  ┌─────────┐ │
│  │タブ切替  │ │
│  └─────────┘ │
│              │
│ ・ダッシュボード │
│ ・ランキング    │
│ ・スコア一覧    │
│ ・スコア入力    │
│ ・一括入力     │
│ ・各賞・ルール  │
│ ・操作方法     │
└──────────────┘
```

### 5.2 レスポンシブ対応

| デバイス | 幅 | 対応 |
|---------|-----|------|
| PC | 769px以上 | 横並びレイアウト |
| タブレット | 481px〜768px | 2カラム |
| スマートフォン | 480px以下 | 1カラム、縦スクロール |

---

## 6. ルール・ロジック

### 6.1 有効ラウンド条件

```javascript
const MIN_PARTICIPANTS = 3; // 有効ラウンドの最小参加人数
```

- 参加人数が3名以上のラウンドのみ有効
- 無効ラウンドはスコア一覧で薄く表示

### 6.2 ランキング対象条件

```javascript
const MIN_ROUNDS = 3; // ランキング対象の最小参加回数
```

- 有効ラウンドに3回以上参加した人のみランキング対象

### 6.3 ハンディキャップ計算

```javascript
// スコアからハンディキャップを減算
const adjustedScore = score - handicaps[user];
```

### 6.4 同率順位の処理

```javascript
// 同じスコア/平均の場合は同じ順位を付与
if (prevAverage !== null && r.average.toFixed(1) !== prevAverage.toFixed(1)) {
    currentRank = i + 1;
}
r.rank = currentRank;
```

### 6.5 各賞の決定ロジック

| 順位 | 賞 | 決定方法 |
|------|-----|----------|
| 1位 | 優勝 | ハンディ適用後の平均スコア最小 |
| 2位 | 準優勝 | 同上2位 |
| 3位 | 3位 | 同上3位 |
| - | ベストスコア賞 | 単発ベストスコア最小 |
| - | パット平均1位 | パット平均最小 |

---

## 7. 年度別ファイル対応（2026/1/5実装）

### 7.1 実施背景

| 課題 | 詳細 |
|------|------|
| 年度選択が固定 | HTMLに直接記述されており、毎年手動修正が必要 |
| データ肥大化 | 全年度のデータが1ファイルに格納 |

### 7.2 変更内容

#### 7.2.1 ファイル構成の変更

| 変更前 | 変更後 |
|--------|--------|
| `data.json`（全年度） | `data-2025.json`, `data-2026.json`（年度別） |
| - | `config.json`（共通設定） |

#### 7.2.2 Lambda API仕様

| メソッド | パラメータ | 説明 |
|----------|-----------|------|
| GET | `?action=config` | 設定取得 |
| GET | `?action=years` | 年度一覧取得 |
| GET | `?year=2025` | 年度データ取得 |
| POST | `?action=config` | 設定保存 |
| POST | （bodyにyear含む） | 年度データ保存 |

#### 7.2.3 IAM権限追加

```json
{
    "Effect": "Allow",
    "Action": "s3:ListBucket",
    "Resource": "arn:aws:s3:::score-manage-micros"
}
```

### 7.3 動作フロー

#### アプリ起動時

```
1. fetchConfig() - 設定取得
2. fetchAvailableYears() - S3のdata-*.jsonから年度自動検出
3. 各年度のデータを取得
4. updateYearSelectors() - 年度セレクト動的生成
5. 現在年度を自動選択
```

#### 新年度の追加（自動）

```
1. ユーザーが2027年を選択してスコア入力
2. data-2027.json が自動作成
3. config.json の availableYears に2027追加
4. 次回アクセス時に2027年が選択肢に表示
```

### 7.4 変更ファイル

| ファイル | 変更内容 |
|----------|----------|
| `index.html` | 年度セレクトを動的生成に変更 |
| `app.js` | 年度別データ管理ロジックに全面改修 |
| `lambda/index.mjs` | 年度別API対応に全面改修 |
| `config.json` | 新規作成 |
| `data-2025.json` | 新規作成（旧data.jsonから分割） |
| `data-2026.json` | 新規作成 |

---

## 8. AWS構成と費用

### 8.1 使用サービス

| サービス | 用途 |
|----------|------|
| S3 | 静的ホスティング + データ保存 |
| Lambda | API（データ読み書き） |
| IAM | Lambda実行ロール |

### 8.2 年間費用見積もり

#### 前提条件

- 6名での利用
- 月間アクセス数：数百回程度
- データサイズ：数十KB程度

#### 費用内訳

| サービス | 月額 | 年額 |
|----------|------|------|
| S3 ストレージ | ~$0.01 | ~$0.12 |
| S3 リクエスト | ~$0.001 | ~$0.01 |
| Lambda | $0（無料枠内） | $0 |
| **合計** | **~$0.01** | **約$0.13（約20円）** |

---

## 9. 運用・保守

### 9.1 新年度への対応

**作業不要** - 自動的に対応されます。

1. 新年になると、現在の年度が自動的に選択肢に追加
2. スコアを入力すると、その年度のデータファイルが自動作成

### 9.2 バックアップ

S3から定期的にダウンロードを推奨：

- `config.json`
- `data-YYYY.json`（各年度）

### 9.3 障害対応

| 症状 | 対応 |
|------|------|
| データが表示されない | ブラウザキャッシュクリア（Ctrl+F5） |
| 保存できない | Lambda関数のCloudWatchログを確認 |
| 年度が表示されない | S3にdata-YYYY.jsonがあるか確認 |

### 9.4 今後の拡張案

- [ ] グラフ表示（スコア推移など）
- [ ] コース別統計
- [ ] 印刷用レイアウト
- [ ] PWA対応（オフライン機能）

---

## 付録：作成工数

| 工程 | 所要時間（概算） |
|------|-----------------|
| 初回実装（2025/12/31） | 約90分 |
| 年度別ファイル対応（2026/1/5） | 約60分 |
| **合計** | **約150分** |

---

以上
