# プロジェクト設計書 - 2026年度ルール対応

**作成日**: 2026年1月8日
**バージョン**: v2.1.0
**作成者**: Claude Sonnet 4.5

---

## 目次

1. [プロジェクト概要](#プロジェクト概要)
2. [更新内容サマリー](#更新内容サマリー)
3. [要件定義](#要件定義)
4. [設計方針](#設計方針)
5. [実装詳細](#実装詳細)
6. [データ構造の変更](#データ構造の変更)
7. [テスト項目](#テスト項目)
8. [既知の制約事項](#既知の制約事項)

---

## プロジェクト概要

### 背景

2026年度より、松本杯ゴルフコンペのルールが大幅に変更されることになりました。主な変更点は以下の通りです：

1. **新しいランキング指標の追加**
   - パーオン率ランキング
   - 平均スコア変化ランキング（昨年度との比較）

2. **賞内容の変更**
   - ベストスコア賞・平均パット賞の廃止
   - カップ名の名前権利の優先順位明確化
   - 昼食代プレゼントのルール変更（1位⇔6位、2位⇔5位）

3. **ハンディキャップ管理の変更**
   - 年2回（1月・6月）にハンディキャップを固定
   - それまでは自動計算を継続

4. **新記録の追加**
   - アルバトロス達成記録

### プロジェクトの目的

既存のゴルフスコア管理Webアプリケーションに、2026年度の新ルールを適用し、新しい機能を実装する。

---

## 更新内容サマリー

| 項目 | 内容 | 影響範囲 |
|------|------|---------|
| パーオン数入力 | スコア入力時にパーオン数（0-18）を入力可能に | HTML, app.js |
| パーオン率ランキング | パーオン率を計算しランキング表示 | HTML, app.js |
| 平均スコア変化ランキング | 昨年度と今年度の平均を比較 | HTML, app.js |
| ハンディキャップ手動更新 | 「更新」ボタンでHCを固定 | HTML, app.js, config.json |
| アルバトロス記録 | HIO・イーグルと同様に記録 | HTML, app.js, lambda/index.mjs |
| ネット/グロス同時表示 | 切り替えではなく並べて表示 | HTML, app.js |
| 2026年度ルール文言 | 賞内容・カップ名ルールの更新 | HTML |
| HC計算ロジック改善 | 20ラウンド未満は全ラウンドからベスト8 | app.js |
| カップ名保存エラー修正 | yearプロパティの確実な設定 | app.js |
| 旧特別賞削除 | ベストスコア賞・平均パット賞を削除 | HTML |

---

## 要件定義

### 機能要件

#### FR-1: パーオン数入力機能

- **要件**: スコア入力時にパーオン数を入力できる
- **入力範囲**: 0〜18
- **必須/任意**: 任意（空欄可）
- **対象画面**:
  - 個人スコア入力
  - 一括スコア入力
  - スコア編集

#### FR-2: パーオン率ランキング

- **計算方法**: `(総パーオン数 / 総ホール数) × 100`
- **表示形式**: パーセンテージ（小数点1桁）
- **条件**: 3回以上参加者のみ
- **ソート**: 降順（高い方が上位）

#### FR-3: 平均スコア変化ランキング

- **計算方法**: `今年度平均スコア - 昨年度平均スコア`
- **表示形式**:
  - マイナス（上達）: 緑色
  - プラス（悪化）: 赤色
- **条件**:
  - 昨年度3回以上参加
  - 今年度3回以上参加
- **ソート**: 昇順（マイナスが大きい方が上位）

#### FR-4: ハンディキャップ手動更新機能

- **機能**: 「ハンディキャップを更新」ボタンで現在のHCを固定
- **実行タイミング**: 年2回（1月・6月）
- **保存先**: config.json の handicaps
- **更新日時記録**: config.json の handicapUpdatedAt
- **確認ダイアログ**: 実行前に確認を求める

#### FR-5: アルバトロス記録機能

- **入力項目**:
  - 達成者
  - ホール番号
  - 日付・コース（ラウンド情報から自動取得）
- **表示場所**:
  - ダッシュボード（アルバトロス達成者リスト）
- **保存先**: yearData.albatrosses 配列

#### FR-6: ネット/グロス同時表示

- **変更点**:
  - 旧: ラジオボタンで切り替え
  - 新: 2つのテーブルを並べて表示
- **表示内容**:
  - ネット（HC適用）: 賞バッジ表示あり
  - グロス（HCなし）: 賞バッジ表示なし

#### FR-7: 2026年度ルール文言更新

**カップ名の名前権利（優先順位）**:
1. ベスト更新（80未満）
2. アルバトロス達成者
3. ホールインワン達成者
4. イーグル達成者
5. グロス1位

**各賞（HC込みアベレージ順位）**:
- 1位: 6位からラウンドの昼食代をプレゼントしてもらう
- 2位: 5位からラウンドの昼食代をプレゼントしてもらう
- 3位: なし
- 4位: なし
- 5位: 2位にラウンドの昼食代をプレゼント
- 6位: 1位にラウンドの昼食代をプレゼント

**特別賞（2026年度〜）**:
- パーオン率ランキング1位
- 平均誤差賞（最も上達した人）

**廃止された賞**:
- ベストスコア賞
- 平均パット数ランキング1位

#### FR-8: HC計算ロジック改善

- **変更点**:
  - 旧: WHSルール（ラウンド数に応じて可変）
  - 新: カスタムルール（20ラウンド未満は全ラウンドからベスト8）
- **計算式（変更なし）**:
  ```
  差分 = (スコア - コースレート) × (113 / スロープレーティング)
  HC = ベスト8の平均差分 × 0.96
  ```

#### FR-9: カップ名保存エラー修正

- **問題**: カップ名変更時に保存失敗
- **原因**: yearData.year プロパティが未設定
- **対策**:
  - yearプロパティを確実に設定
  - appStateに明示的に反映

### 非機能要件

#### NFR-1: 互換性

- **後方互換性**: 既存データ（パーオンなし）も正常に表示
- **Lambda互換性**: Lambda関数でalbatrosses配列を返す

#### NFR-2: パフォーマンス

- **計算負荷**: ランキング計算は既存と同程度
- **レスポンス**: ボタン操作は1秒以内に完了

#### NFR-3: ユーザビリティ

- **入力検証**: パーオン数は0-18の範囲チェック
- **エラーメッセージ**: 保存失敗時は明確なメッセージ表示
- **確認ダイアログ**: HC更新時は確認を求める

---

## 設計方針

### アーキテクチャ方針

1. **既存構造の維持**:
   - 既存のコード構造を維持し、最小限の変更で実装
   - モジュール分離は既存パターンに従う

2. **データ拡張性**:
   - 既存データ構造を破壊しない形で拡張
   - 新フィールドはすべて任意（undefined可）

3. **UI一貫性**:
   - 既存のCSSクラスを最大限活用
   - 新規スタイルは最小限に抑える

### データフロー

```
[ユーザー入力]
    ↓
[スコア保存] → yearData.rounds[].scores[user].parOn に保存
    ↓
[LocalStorage保存] → golfScoreApp キーで保存
    ↓
[Lambda同期] → S3 data-YYYY.json に保存
    ↓
[ランキング計算] → パーオン率・スコア変化を計算
    ↓
[画面表示] → ランキング表示
```

### エラーハンドリング

1. **カップ名保存エラー**:
   - try-catchで捕捉
   - エラー時はalertで通知
   - console.errorにログ出力

2. **ランキング計算エラー**:
   - データ不足時は「データがありません」表示
   - nullチェックを確実に実施

3. **HC更新エラー**:
   - 保存失敗時はalertで通知
   - HC値は変更しない（ロールバック不要）

---

## 実装詳細

### 1. パーオン数入力機能

#### HTML変更

**個人スコア入力（my-input-paron）**:
```html
<div class="form-group">
    <label for="my-input-paron">パーオン:</label>
    <input type="number" id="my-input-paron" placeholder="例: 12" min="0" max="18" inputmode="numeric">
</div>
```

**一括スコア入力（paron-{user}）**:
```html
<input type="number" id="paron-${user}" placeholder="パーオン" min="0" max="18" inputmode="numeric" value="${scoreData.parOn || ''}">
```

#### JavaScript変更

**個人スコア保存（saveMyScore）**:
```javascript
const parOn = parseInt(document.getElementById('my-input-paron')?.value);
// ...
if (parOn) scoreEntry.parOn = parOn;
```

**一括スコア保存（saveScore）**:
```javascript
const parOn = parseInt(document.getElementById(`paron-${user}`)?.value);
// ...
if (parOn) scores[user].parOn = parOn;
```

### 2. パーオン率ランキング

#### 計算ロジック（updateParOnRanking）:

```javascript
function updateParOnRanking(rounds) {
    const tbody = document.querySelector('#paron-ranking tbody');
    if (!tbody) return;

    const validRounds = rounds.filter(r => countParticipants(r) >= MIN_PARTICIPANTS);

    const rankings = USERS.map(user => {
        const userRounds = validRounds.filter(r =>
            r.scores[user] && r.scores[user].parOn !== undefined
        );
        const parOns = userRounds.map(r => r.scores[user].parOn);

        const totalParOns = parOns.reduce((a, b) => a + b, 0);
        const totalHoles = userRounds.length * 18;
        const parOnRate = parOns.length >= MIN_ROUNDS
            ? (totalParOns / totalHoles) * 100
            : null;

        return {
            user,
            rounds: userRounds.length,
            parOnRate,
            isValid: parOns.length >= MIN_ROUNDS
        };
    }).filter(r => r.isValid)
      .sort((a, b) => b.parOnRate - a.parOnRate); // 降順

    // ランキング表示処理...
}
```

#### HTML表示:

```html
<div class="card">
    <h3>パーオン率ランキング⭐️</h3>
    <p class="note">※3回以上参加者のみ</p>
    <table class="ranking-table" id="paron-ranking">
        <thead>
            <tr>
                <th>順位</th>
                <th>名前</th>
                <th>パーオン率</th>
                <th>回数</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>
```

### 3. 平均スコア変化ランキング

#### 計算ロジック（updateScoreChangeRanking）:

```javascript
function updateScoreChangeRanking() {
    const tbody = document.querySelector('#score-change-ranking tbody');
    if (!tbody) return;

    const currentYear = appState.currentYear;
    const lastYear = currentYear - 1;

    const currentYearData = appState.yearData[currentYear];
    const lastYearData = appState.yearData[lastYear];

    if (!currentYearData || !lastYearData) {
        tbody.innerHTML = '<tr><td colspan="5" class="no-data">昨年度のデータがありません</td></tr>';
        return;
    }

    const currentValidRounds = currentYearData.rounds.filter(r =>
        countParticipants(r) >= MIN_PARTICIPANTS
    );
    const lastValidRounds = lastYearData.rounds.filter(r =>
        countParticipants(r) >= MIN_PARTICIPANTS
    );

    const rankings = USERS.map(user => {
        // 今年の平均計算
        const currentScores = currentValidRounds
            .filter(r => r.scores[user])
            .map(r => r.scores[user].score);
        const currentAvg = currentScores.length >= MIN_ROUNDS
            ? currentScores.reduce((a, b) => a + b, 0) / currentScores.length
            : null;

        // 昨年の平均計算
        const lastScores = lastValidRounds
            .filter(r => r.scores[user])
            .map(r => r.scores[user].score);
        const lastAvg = lastScores.length >= MIN_ROUNDS
            ? lastScores.reduce((a, b) => a + b, 0) / lastScores.length
            : null;

        const change = (currentAvg !== null && lastAvg !== null)
            ? currentAvg - lastAvg
            : null;

        return {
            user,
            lastAvg,
            currentAvg,
            change,
            isValid: change !== null
        };
    }).filter(r => r.isValid)
      .sort((a, b) => a.change - b.change); // 昇順（マイナスが上位）

    // ランキング表示処理...
    // changeの符号によって色分け
    const changeClass = change < 0 ? 'score-improved' : 'score-worse';
}
```

#### CSS追加:

```css
/* スコア変化の色分け */
.score-improved {
    color: var(--primary-color);
    font-weight: bold;
}

.score-worse {
    color: var(--danger);
    font-weight: bold;
}
```

### 4. ハンディキャップ手動更新機能

#### HTML追加:

```html
<div class="button-group">
    <button id="update-handicap-btn" class="btn-primary">ハンディキャップを更新</button>
</div>
```

#### JavaScript実装:

```javascript
async function updateAndSaveHandicaps() {
    const confirmMsg = 'ハンディキャップを更新しますか？\n\n直近20ラウンドのベスト8から計算された値がconfig.jsonに保存されます。\n※この操作は1月と6月に実施してください。';

    if (!confirm(confirmMsg)) {
        return;
    }

    // 全ユーザーのハンディキャップを計算
    USERS.forEach(user => {
        const hcData = calculateHandicapIndex(user);
        appState.config.handicaps[user] = hcData.index;
    });

    // ハンディキャップ更新日時を記録
    const now = new Date();
    appState.config.handicapUpdatedAt = now.toISOString();

    try {
        await saveConfig();
        updateHandicapView();
        alert(`ハンディキャップを更新しました\n更新日時: ${now.toLocaleString('ja-JP')}`);
    } catch (error) {
        console.error('ハンディキャップ保存エラー:', error);
        alert('ハンディキャップの保存に失敗しました');
    }
}

// イベントリスナー登録
document.getElementById('update-handicap-btn')?.addEventListener('click', updateAndSaveHandicaps);
```

### 5. アルバトロス記録機能

#### HTML追加（一括入力画面）:

```html
<div class="form-group">
    <label for="albatross-input">アルバトロス:</label>
    <select id="albatross-input">
        <option value="">なし</option>
    </select>
    <input type="number" id="albatross-hole" placeholder="ホール番号" min="1" max="18" inputmode="numeric">
</div>
```

#### HTML追加（ダッシュボード）:

```html
<div class="achievement-item">
    <h4>アルバトロス達成者</h4>
    <ul id="albatross-list">
        <li class="no-data">達成者なし</li>
    </ul>
</div>
```

#### JavaScript実装（saveScore）:

```javascript
const albatrossUser = document.getElementById('albatross-input')?.value;
const albatrossHole = document.getElementById('albatross-hole')?.value;
if (albatrossUser && albatrossHole) {
    yearData.albatrosses.push({
        user: albatrossUser,
        date,
        course,
        hole: parseInt(albatrossHole)
    });
}
```

#### Lambda対応（lambda/index.mjs）:

```javascript
return {
    year: parseInt(year),
    rounds: [],
    holeInOnes: [],
    eagles: [],
    albatrosses: [],  // 追加
    cupName: "松本杯"
};
```

### 6. ネット/グロス同時表示

#### HTML変更:

**ネットランキング**:
```html
<div class="card">
    <h3>総合順位（ネット - HC適用）</h3>
    <p class="note">※3回以上参加者のみ</p>
    <table class="ranking-table" id="overall-ranking-net">
        <thead>
            <tr>
                <th>順位</th>
                <th>名前</th>
                <th>平均</th>
                <th>回数</th>
                <th>賞</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>
```

**グロスランキング**:
```html
<div class="card">
    <h3>総合順位（グロス - HCなし）</h3>
    <p class="note">※3回以上参加者のみ</p>
    <table class="ranking-table" id="overall-ranking-gross">
        <thead>
            <tr>
                <th>順位</th>
                <th>名前</th>
                <th>平均</th>
                <th>回数</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>
```

#### JavaScript変更（updateRankings）:

```javascript
function updateRankings() {
    const yearData = getCurrentYearData();
    if (!yearData || !yearData.rounds) return;

    // ネット（HC適用）とグロス（HCなし）の両方を表示
    updateOverallRanking(yearData.rounds, true, 'overall-ranking-net');  // ネット
    updateOverallRanking(yearData.rounds, false, 'overall-ranking-gross'); // グロス
    updateBestScoreRanking(yearData.rounds, true);
    updatePuttRanking(yearData.rounds);
    updateParOnRanking(yearData.rounds);
    updateScoreChangeRanking();
}
```

### 7. カップ名保存エラー修正

#### 修正内容（saveCupName）:

```javascript
async function saveCupName() {
    const input = document.getElementById('cup-name-input');
    const cupName = input.value.trim();

    if (!cupName) {
        alert('カップ名を入力してください');
        return;
    }

    const year = appState.currentYear;
    const yearData = getCurrentYearData();

    // カップ名を更新
    yearData.cupName = cupName;

    // 年度プロパティが存在することを確認（Lambda送信用）
    if (!yearData.year) {
        yearData.year = parseInt(year);
    }

    // appStateに反映（重要！）
    appState.yearData[year] = yearData;

    try {
        await saveYearData(year);
        updateCupName();
        alert('カップ名を保存しました');
    } catch (error) {
        console.error('カップ名保存エラー:', error);
        alert('カップ名の保存に失敗しました');
    }
}
```

**変更点**:
1. カップ名更新を先に実施
2. yearプロパティ確認を明示
3. appStateへの反映を追加

### 8. HC計算ロジック改善

#### 修正内容（calculateHandicapIndex）:

```javascript
// ベスト採用数を決定
// 20回未満: 全ラウンドからベスト8（ただし全ラウンド数が8未満の場合は全て）
// 20回以上: 直近20からベスト8
const numBest = Math.min(8, targetRounds.length);
```

**変更点**:
- 旧: WHSルール（ラウンド数に応じて1〜8で可変）
- 新: 常にベスト8（ただし8未満の場合は全て）

---

## データ構造の変更

### yearData構造（data-YYYY.json）

```json
{
  "year": 2026,
  "rounds": [
    {
      "roundNumber": 1,
      "date": "2026-01-15",
      "course": "箱根くらかけ",
      "scores": {
        "松本": {
          "score": 88,
          "putt": 34,
          "parOn": 12  // ⭐ 新規追加
        }
      }
    }
  ],
  "holeInOnes": [],
  "eagles": [],
  "albatrosses": [  // ⭐ 新規追加
    {
      "user": "松本",
      "date": "2026-01-15",
      "course": "箱根くらかけ",
      "hole": 5
    }
  ],
  "cupName": "松本杯"
}
```

### config.json構造

```json
{
  "availableYears": [2025, 2026],
  "handicaps": {
    "松本": 12.5,
    "正本": 15.2,
    ...
  },
  "handicapUpdatedAt": "2026-01-08T00:00:00.000Z",  // ⭐ 新規追加
  "courses": {
    "サカワ": { "rating": 72.0, "slope": 113, "par": 72 },
    ...
  }
}
```

### 新規フィールド一覧

| フィールド | 型 | 親オブジェクト | 説明 |
|-----------|-----|--------------|------|
| parOn | number | rounds[].scores[user] | パーオン数（0-18） |
| albatrosses | array | yearData | アルバトロス達成記録 |
| handicapUpdatedAt | string (ISO8601) | config | HC更新日時 |

---

## テスト項目

### 単体テスト

#### パーオン率計算

| ケース | 入力 | 期待結果 |
|--------|------|---------|
| 正常: 3ラウンド | parOn: [12, 14, 10] | 66.7% |
| 境界: 0パーオン | parOn: [0, 0, 0] | 0.0% |
| 境界: 全パーオン | parOn: [18, 18, 18] | 100.0% |
| 異常: 2ラウンド | parOn: [12, 14] | ランキング対象外 |
| 異常: parOn未入力 | parOn: [undefined, undefined] | ランキング対象外 |

#### スコア変化計算

| ケース | 昨年平均 | 今年平均 | 期待結果 |
|--------|---------|---------|---------|
| 上達 | 100 | 95 | -5.0（緑色） |
| 悪化 | 100 | 105 | +5.0（赤色） |
| 変化なし | 100 | 100 | 0.0 |
| 昨年データなし | - | 95 | 表示なし |
| 今年データなし | 100 | - | 表示なし |

#### HC計算（カスタムルール）

| ラウンド数 | 対象 | 採用数 |
|-----------|------|--------|
| 5 | 全5ラウンド | ベスト5 |
| 10 | 全10ラウンド | ベスト8 |
| 15 | 全15ラウンド | ベスト8 |
| 20 | 直近20ラウンド | ベスト8 |
| 25 | 直近20ラウンド | ベスト8 |

### 結合テスト

#### シナリオ1: スコア入力→ランキング表示

1. ログイン
2. スコア入力でparOn=12を入力
3. 保存成功
4. ランキングタブを開く
5. パーオン率ランキングに反映されていることを確認

#### シナリオ2: HC手動更新

1. ログイン
2. ハンディキャップタブを開く
3. 「ハンディキャップを更新」ボタンをクリック
4. 確認ダイアログで「OK」
5. 成功メッセージ表示
6. config.jsonにHC値が保存されていることを確認

#### シナリオ3: カップ名変更

1. ログイン
2. 各賞・ルールタブを開く
3. カップ名を「比企杯」に変更
4. 保存ボタンをクリック
5. 成功メッセージ表示
6. ページリロード後もカップ名が保持されていることを確認

### E2Eテスト（ブラウザ）

| 項目 | Chrome | Safari | Edge |
|------|--------|--------|------|
| スコア入力（パーオン） | ✅ | ✅ | ✅ |
| パーオン率ランキング表示 | ✅ | ✅ | ✅ |
| スコア変化ランキング表示 | ✅ | ✅ | ✅ |
| HC更新ボタン | ✅ | ✅ | ✅ |
| アルバトロス入力 | ✅ | ✅ | ✅ |
| カップ名変更 | ✅ | ✅ | ✅ |

---

## 既知の制約事項

### 制約1: 既存データのパーオン

- **制約**: 過去のラウンドにはparOnデータが存在しない
- **影響**: 初期はパーオン率ランキングの参加者が少ない
- **対応**: 必要に応じて手動でparOnを追加編集

### 制約2: 昨年度データの必要性

- **制約**: スコア変化ランキングは昨年度データが必須
- **影響**: 2026年は2025年データが必要
- **対応**: 2025年データがない場合は「データがありません」表示

### 制約3: HC更新の手動実行

- **制約**: HC更新は手動実行（自動実行なし）
- **影響**: 1月・6月に実行忘れの可能性
- **対応**: 操作方法に実行タイミングを明記

### 制約4: Lambda関数の更新

- **制約**: アルバトロス対応にはLambda関数の更新が必要
- **影響**: Lambda未更新の場合、albatrossesが返されない
- **対応**: UPDATE_PROCEDURE.mdに手順を明記

### 制約5: ブラウザキャッシュ

- **制約**: ブラウザキャッシュにより古いコードが実行される
- **影響**: 更新後も旧バージョンが動作する
- **対応**: キャッシュクリア（Ctrl+F5）の案内

---

## まとめ

### 実装完了項目

- ✅ パーオン数入力機能
- ✅ パーオン率ランキング
- ✅ 平均スコア変化ランキング
- ✅ ハンディキャップ手動更新機能
- ✅ アルバトロス記録機能
- ✅ ネット/グロス同時表示
- ✅ 2026年度ルール文言更新
- ✅ HC計算ロジック改善
- ✅ カップ名保存エラー修正
- ✅ 旧特別賞削除（ベストスコア賞・平均パット賞）

### 今後の課題

- [ ] コースレート・スロープの実データ取得
- [ ] グラフ表示（スコア推移）
- [ ] コース別統計
- [ ] PWA対応（オフライン機能）

### 注意事項

1. **S3へのアップロード**: index.html, app.js, styles.cssを必ずアップロード
2. **Lambda関数更新**: lambda/index.mjsを更新（albatrosses対応）
3. **ブラウザキャッシュクリア**: 更新後は必ずキャッシュクリア
4. **HC更新の実行**: 1月・6月に忘れずに実行

---

**以上**
