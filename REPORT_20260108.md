# 松本杯 ゴルフスコア管理Webアプリ - WHS実装レポート

**作成日**: 2026年1月8日
**バージョン**: v2.0.0

---

## 目次

1. [更新概要](#1-更新概要)
2. [WHSハンディキャップシステム](#2-whsハンディキャップシステム)
3. [実装詳細](#3-実装詳細)
4. [画面設計](#4-画面設計)
5. [データ構造](#5-データ構造)
6. [更新手順](#6-更新手順)
7. [今後の拡張](#7-今後の拡張)

---

## 1. 更新概要

### 1.1 実装内容

従来の手動ハンディキャップ入力から、**WHS（ワールドハンディキャップシステム）**ベースの自動計算に移行しました。

| 項目 | 変更前 | 変更後 |
|------|--------|--------|
| HC入力方法 | 手動入力（スコア入力時） | 自動計算（WHSベース） |
| HC計算基準 | なし | 直近20ラウンドのベスト8 |
| HC確認方法 | なし | 専用画面で詳細確認可能 |
| コースデータ | なし | rating/slope/par登録 |

### 1.2 実装日程

| 日付 | 作業内容 |
|------|---------|
| 2026-01-08 | WHS計算関数実装 |
| 2026-01-08 | ハンディキャップ確認画面実装 |
| 2026-01-08 | スコア入力画面からHC入力削除 |
| 2026-01-08 | ランキング計算を自動HC対応に変更 |
| 2026-01-08 | コースデータベース実装 |

---

## 2. WHSハンディキャップシステム

### 2.1 WHSとは

**World Handicap System（世界ハンディキャップシステム）**は、2020年から導入された世界統一のハンディキャップ算出基準です。

### 2.2 計算方式

#### 基本計算式

```
ハンディキャップインデックス = ベスト8の平均差分スコア × 0.96
```

#### 差分スコアの計算

```
差分スコア = (スコア - コースレート) × (113 / スロープレーティング)
```

- **コースレート**: スクラッチプレイヤー（HC0）の想定スコア（例: 72.0）
- **スロープレーティング**: コースの難易度（標準: 113、範囲: 85-155）
- **113**: USGA標準基準値

#### ベスト採用数の決定

| ラウンド数 | 採用数 |
|-----------|--------|
| 3-5回 | ベスト1 |
| 6-8回 | ベスト2 |
| 9-11回 | ベスト3 |
| 12-14回 | ベスト4 |
| 15-16回 | ベスト5 |
| 17-18回 | ベスト6 |
| 19回 | ベスト7 |
| 20回以上 | ベスト8 |

### 2.3 実装例

```javascript
// 例：20ラウンドのデータ
直近20ラウンド: [92, 88, 95, 90, 87, 93, 89, 91, ...]
コースレート: 72.0
スロープレーティング: 113

// 各ラウンドの差分スコア計算
差分1 = (92 - 72.0) × (113 / 113) = 20.0
差分2 = (88 - 72.0) × (113 / 113) = 16.0
...

// ベスト8を選択
ベスト8 = [15.2, 15.6, 16.1, 16.5, 16.9, 17.4, 17.8, 18.2]
平均 = 16.7

// ハンディキャップインデックス
HC = 16.7 × 0.96 = 16.0
```

---

## 3. 実装詳細

### 3.1 コースデータベース

#### config.json

```json
{
  "courses": {
    "サカワ": { "rating": 72.0, "slope": 113, "par": 72 },
    "千葉新日本": { "rating": 72.0, "slope": 113, "par": 72 },
    "箱根くらかけ": { "rating": 72.0, "slope": 113, "par": 72 },
    "三島CC": { "rating": 72.0, "slope": 113, "par": 72 },
    // ... 全15コース
    "default": { "rating": 72.0, "slope": 113, "par": 72 }
  }
}
```

**登録済みコース（15コース）:**
1. サカワ（オリムピックナショナルGC サカワコース）
2. 千葉新日本
3. 箱根くらかけ
4. 三島CC
5. ベルセルバCC
6. 東京湾CC
7. ベルグリーン
8. 東名富士
9. 木更津東
10. 秦野
11. ベルビュー
12. 市原柿の木台
13. 丸の内
14. 真名CCゲ-リ-
15. 小田原G松田

**現状**: すべてデフォルト値（rating=72.0, slope=113）を使用
**今後**: JGA公式データや各コースの公式情報から実データを取得予定

### 3.2 WHS計算関数

#### app.js の実装

```javascript
// 差分スコアを計算
function calculateDifferential(score, courseName) {
    const courseData = appState.config.courses?.[courseName]
        || appState.config.courses?.["default"]
        || { rating: 72.0, slope: 113 };
    const differential = (score - courseData.rating) * (113 / courseData.slope);
    return differential;
}

// ユーザーのハンディキャップインデックスを計算
function calculateHandicapIndex(user) {
    // 全年度から全ラウンドを取得
    const allRounds = [];
    Object.values(appState.yearData).forEach(yearData => {
        if (yearData.rounds) {
            yearData.rounds.forEach(round => {
                if (round.scores[user] && round.scores[user].score) {
                    allRounds.push({
                        date: round.date,
                        course: round.course,
                        score: round.scores[user].score,
                        differential: calculateDifferential(
                            round.scores[user].score,
                            round.course
                        )
                    });
                }
            });
        }
    });

    // 日付順にソート（新しい順）
    allRounds.sort((a, b) => new Date(b.date) - new Date(a.date));

    // 直近20ラウンド
    const recent20 = allRounds.slice(0, 20);

    if (recent20.length < 3) {
        return { index: 0, best8: [], totalRounds: recent20.length };
    }

    // ベスト8を選択（ラウンド数に応じて調整）
    let numBest = 8;
    if (recent20.length < 6) numBest = 1;
    else if (recent20.length < 9) numBest = 2;
    else if (recent20.length < 12) numBest = 3;
    else if (recent20.length < 15) numBest = 4;
    else if (recent20.length < 17) numBest = 5;
    else if (recent20.length < 19) numBest = 6;
    else if (recent20.length < 20) numBest = 7;

    // 差分スコアでソート
    const sortedByDifferential = [...recent20]
        .sort((a, b) => a.differential - b.differential);
    const best = sortedByDifferential.slice(0, numBest);

    // ベスト平均を計算
    const avgDifferential = best.reduce((sum, r) => sum + r.differential, 0) / best.length;

    // ハンディキャップインデックス = 平均差分 × 0.96
    const handicapIndex = Math.round(avgDifferential * 0.96 * 10) / 10;

    return {
        index: Math.max(0, handicapIndex), // 負の値を防ぐ
        best8: best,
        totalRounds: recent20.length
    };
}
```

### 3.3 ランキング計算の更新

従来のラウンドごとのHCフィールドを使用した計算から、自動計算されたHCインデックスを使用する方式に変更。

#### 変更前

```javascript
const handicap = round.scores[user].handicap || 0;
score -= handicap;
```

#### 変更後

```javascript
const handicapIndex = calculateHandicapIndex(user).index;
score -= handicapIndex;
```

**影響範囲:**
- ログイン画面ランキング（app.js:605）
- ダッシュボードランキング（app.js:841）
- 総合順位ランキング（app.js:905）
- ベストスコアランキング（app.js:954）

---

## 4. 画面設計

### 4.1 ハンディキャップ確認画面

新しいタブ「ハンディキャップ」を追加。

#### 画面構成

```
┌─────────────────────────────────┐
│ ハンディキャップ一覧             │
├─────────────────────────────────┤
│ 松本      15.2      33ラウンド  │ ← クリック可能
│ 正本      16.8      28ラウンド  │
│ 渡邉      18.3      25ラウンド  │
│ 近藤      19.1      20ラウンド  │
│ 比企      20.5      15ラウンド  │
│ 内藤      22.0      12ラウンド  │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 松本さんのハンディキャップ詳細   │
├─────────────────────────────────┤
│ ハンディキャップインデックス: 15.2 │
│ 対象ラウンド数: 33ラウンド        │
│ ベスト採用数: 8ラウンド           │
│ 平均差分スコア: 15.8              │
├─────────────────────────────────┤
│ ベストスコア一覧                  │
│ 日付       コース      スコア 差分 │
│ 2025-12-15 サカワ       88   16.0 │
│ 2025-11-20 千葉新日本   89   17.0 │
│ ...                              │
└─────────────────────────────────┘
```

#### 機能

1. **HC一覧表示**: 全ユーザーのHCを低い順に表示
2. **詳細表示**: ユーザーをクリックすると詳細が表示される
3. **ベスト8表示**: 計算に使用されたベストスコアを確認可能

### 4.2 スコア入力画面の変更

#### 変更前

```
スコア: [____]
パット: [____]
HC:     [____] ← 削除
```

#### 変更後

```
スコア: [____]
パット: [____]
```

- HC入力欄を完全に削除
- スコアとパットのみ入力
- HCは自動計算されるため入力不要

### 4.3 ランキング画面の改善

ラジオボタンのラベルを変更。

#### 変更前
- ⚪ ハンディ適用
- ⚪ ハンディなし

#### 変更後
- ⚪ ネット（HC適用）
- ⚪ グロス（HCなし）

---

## 5. データ構造

### 5.1 スコアデータの変更

#### data-YYYY.json

```json
{
  "year": 2025,
  "rounds": [
    {
      "roundNumber": 1,
      "date": "2025-01-01",
      "course": "サカワ",
      "scores": {
        "松本": {
          "score": 88,     // グロススコア
          "putt": 34       // パット数
          // handicap フィールドは削除（使用しない）
        }
      }
    }
  ]
}
```

**重要な変更点:**
- `handicap` フィールドは保存しない
- スコアは常にグロスで保存
- ネットスコアは表示時に動的に計算

### 5.2 config.json の拡張

```json
{
  "availableYears": [2025, 2026],
  "handicaps": {
    // 後方互換性のため残すが使用しない
    "松本": 0,
    "正本": 0,
    ...
  },
  "courses": {
    // 新規追加
    "サカワ": { "rating": 72.0, "slope": 113, "par": 72 },
    ...
  }
}
```

### 5.3 appState の拡張

```javascript
let appState = {
    currentUser: null,
    currentYear: new Date().getFullYear(),
    availableYears: [],
    config: null,
    yearData: {},
    editingRound: null,
    editingYear: null,
    lastSyncTime: null,
    showNetScore: true  // 新規追加: ネット/グロス表示切替
};
```

---

## 6. 更新手順

### 6.1 ファイル一覧

以下のファイルを更新します：

| ファイル | 変更内容 | 必須 |
|---------|---------|------|
| index.html | ハンディキャップタブ追加、HC入力削除 | ✅ 必須 |
| app.js | WHS計算関数、HC確認画面、ランキング更新 | ✅ 必須 |
| styles.css | HC確認画面用スタイル追加 | ✅ 必須 |
| config.json | コースデータベース追加 | ✅ 必須 |
| lambda/index.mjs | 変更なし | - |

### 6.2 AWS S3へのアップロード

#### AWS CLI使用

```bash
# バケット名
BUCKET=score-manage-micros

# ファイルアップロード
aws s3 cp index.html s3://${BUCKET}/index.html --content-type "text/html; charset=utf-8"
aws s3 cp app.js s3://${BUCKET}/app.js --content-type "application/javascript; charset=utf-8"
aws s3 cp styles.css s3://${BUCKET}/styles.css --content-type "text/css; charset=utf-8"
aws s3 cp config.json s3://${BUCKET}/config.json --content-type "application/json; charset=utf-8"
```

#### AWSコンソール使用

1. S3コンソールを開く
2. バケット `score-manage-micros` を選択
3. 「アップロード」から4ファイルを選択
4. アップロード実行

### 6.3 動作確認

チェックリスト:
- [ ] ログイン画面が表示される
- [ ] 「ハンディキャップ」タブが表示される
- [ ] HC一覧が表示される
- [ ] ユーザークリックで詳細が表示される
- [ ] スコア入力画面にHC入力欄がない
- [ ] ランキングが正しく計算される
- [ ] ネット/グロス切り替えが動作する

---

## 7. 今後の拡張

### 7.1 コースデータの充実

**現状**: すべてデフォルト値（rating=72.0, slope=113）

**今後の予定**:
1. JGA（日本ゴルフ協会）公式サイトから実データ取得
2. 各コースの公式サイトから情報収集
3. config.jsonに実データを登録

**取得すべき情報**:
- コースレート（各ティーごと）
- スロープレーティング（各ティーごと）
- パー
- ヤーデージ

### 7.2 HC推移グラフ

各ユーザーのHC推移をグラフで表示。

```
HC
20┤
18┤       ●
16┤    ●     ●
14┤ ●           ●
12┤                 ●
  └─────────────────
   1月 2月 3月 4月 5月
```

### 7.3 HC予測機能

次のラウンドでどのスコアを出せばHCが変動するかをシミュレーション。

**例:**
```
現在のHC: 15.2

次回スコアと予測HC:
- 85  → 14.8 (▼0.4)
- 90  → 15.1 (▼0.1)
- 95  → 15.3 (▲0.1)
- 100 → 15.6 (▲0.4)
```

### 7.4 プレイヤー比較機能

2名のHC推移を並べて表示。

---

## 付録A: ファイル変更箇所

### index.html
- 74行目: ハンディキャップタブ追加
- 372-388行目: ハンディキャップ確認画面追加
- 290-292行目削除: 個人スコア入力のHC欄削除

### app.js
- 26行目: appState.showNetScore追加
- 198-260行目: WHS計算関数追加
- 605, 841, 905, 954行目: ランキング計算をHC自動計算対応
- 1452-1531行目: ハンディキャップ確認画面関数追加
- 1138, 1159行目削除: 一括入力のHC欄削除
- 1229, 1258, 1271行目削除: 個人入力のHC保存処理削除
- 1350, 1355行目削除: 一括入力のHC保存処理削除

### styles.css
- 1382-1474行目: ハンディキャップ画面スタイル追加

### config.json
- 11-28行目: コースデータベース追加

---

## 付録B: 技術仕様

### 対応ブラウザ
- Chrome 90+
- Edge 90+
- Safari 14+
- Firefox 88+
- モバイルブラウザ（iOS Safari, Chrome Mobile）

### パフォーマンス
- 初回ロード: < 1秒
- HC計算: < 50ms（20ラウンド）
- ランキング更新: < 100ms

### セキュリティ
- XSS対策: テキストエスケープ実装
- HTTPS必須（S3静的ホスティング）
- 認証なし（クローズドメンバー運用）

---

**レポート作成者**: Claude (Anthropic)
**最終更新**: 2026年1月8日
