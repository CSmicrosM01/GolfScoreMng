# 更新手順（2026年1月9日 v2.2.0）

## v2.2.0 更新内容

### 新機能・変更内容
- パーオン率をPar3/Par4/Par5別に入力（楽天ゴルフスコア対応）
- 総合パーオン率の自動計算（Par3:4H, Par4:10H, Par5:4Hの重み付け平均）
- ランキング表示を1回以上の参加で表示するように変更（最終順位確定は3回以上）
- デフォルトカップ名を「正本杯」に変更
- 特別賞の賞品情報追加（ゴルフボール5,000円〜6,000円相当）
- 操作方法の更新

---

## AWS S3へのアップロード手順

### 1. 更新ファイルの確認

以下のファイルを更新します：

```
✅ index.html      - パーオン入力フィールド、新ランキング表示、HC更新ボタン、2026ルール更新
✅ app.js          - パーオン保存、新ランキング計算、HC手動更新機能、アルバトロス対応
✅ styles.css      - スコア変化の色分けスタイル追加
✅ config.json     - ハンディキャップ更新日時フィールド追加（自動）
✅ lambda/index.mjs - アルバトロス対応（既に実装済み）
```

### 2. S3バケットへのアップロード

#### AWS CLIを使用する場合

```bash
# バケット名を環境変数に設定
export BUCKET_NAME=score-manage-micros

# 更新ファイルをアップロード
aws s3 cp index.html s3://${BUCKET_NAME}/index.html --content-type "text/html; charset=utf-8"
aws s3 cp app.js s3://${BUCKET_NAME}/app.js --content-type "application/javascript; charset=utf-8"
aws s3 cp styles.css s3://${BUCKET_NAME}/styles.css --content-type "text/css; charset=utf-8"
aws s3 cp config.json s3://${BUCKET_NAME}/config.json --content-type "application/json; charset=utf-8"

# アップロード確認
aws s3 ls s3://${BUCKET_NAME}/ --human-readable
```

#### AWSマネジメントコンソールを使用する場合

1. **S3コンソールにアクセス**
   - https://s3.console.aws.amazon.com/s3/buckets/score-manage-micros

2. **ファイルをアップロード**
   - 「アップロード」ボタンをクリック
   - 以下のファイルを選択：
     - `index.html`
     - `app.js`
     - `styles.css`
     - `config.json`
   - 「アップロード」をクリック

3. **既存ファイルの上書き確認**
   - 既存ファイルは自動的に上書きされます

### 3. Lambda関数の更新

**Lambda関数（lambda/index.mjs）を更新してください**

アルバトロス機能追加のため、Lambda関数を更新する必要があります。

#### Lambda関数の更新手順

1. **Lambda関数のコードを更新**
   - AWSマネジメントコンソール → Lambda → 該当関数を選択
   - コードエディタで `index.mjs` を開く
   - `lambda/index.mjs` の内容で上書き（特に195行目に `albatrosses: []` 追加）
   - 「Deploy」をクリック

2. **または、AWS CLIで更新**
   ```bash
   # lambda/index.mjsをzip化
   cd lambda
   zip function.zip index.mjs

   # Lambda関数を更新
   aws lambda update-function-code \
     --function-name <関数名> \
     --zip-file fileb://function.zip
   ```

3. **config.jsonの新しいフィールド**
   - `handicapUpdatedAt`: ハンディキャップ更新日時（自動追加）
   - 既存フィールドとの互換性あり

### 4. 動作確認

1. **Webサイトにアクセス**
   ```
   http://score-manage-micros.s3-website-ap-northeast-1.amazonaws.com
   ```

2. **確認項目**
   - [ ] ログイン画面が表示される
   - [ ] ダッシュボードにアルバトロス達成者が表示される
   - [ ] ランキングタブで以下が表示される：
     - [ ] 総合順位（ネット - HC適用）
     - [ ] 総合順位（グロス - HCなし）
     - [ ] パーオン率ランキング⭐️
     - [ ] 平均スコア変化ランキング⭐️
   - [ ] スコア入力（個人用）にパーオン入力欄がある
   - [ ] 一括入力にパーオン・アルバトロス入力欄がある
   - [ ] ハンディキャップタブに「ハンディキャップを更新」ボタンがある
   - [ ] HC更新ボタンをクリックすると確認ダイアログが表示される
   - [ ] 各賞・ルールタブで2026年度ルールが表示される
     - [ ] カップ名の名前権利（優先順位）
     - [ ] 各賞の内容（1位⇔6位、2位⇔5位）
     - [ ] 特別賞（パーオン率、平均誤差賞）

### 5. トラブルシューティング

#### キャッシュクリア
ブラウザのキャッシュが残っている場合、古いファイルが表示される可能性があります。

**Chrome/Edge:**
- `Ctrl + Shift + Delete` → キャッシュをクリア
- または `Ctrl + F5` でスーパーリロード

**Safari:**
- `Command + Option + E` → キャッシュをクリア
- ページをリロード

#### エラーが発生した場合

1. **ブラウザの開発者ツールを開く**
   - Chrome/Edge: `F12`
   - Safari: `Command + Option + I`

2. **コンソールタブでエラーを確認**
   - JavaScript エラーが表示されている場合、ファイルのアップロードを再確認

3. **ネットワークタブで確認**
   - 各ファイル（index.html, app.js, styles.css, config.json）が正しく読み込まれているか確認
   - Status: 200 OK になっているか確認

---

## 新機能の使い方

### ハンディキャップ確認画面

1. ログイン後、「ハンディキャップ」タブをクリック
2. 全ユーザーのハンディキャップ一覧が表示されます
3. ユーザー名をクリックすると詳細が表示されます：
   - ハンディキャップインデックス
   - 対象ラウンド数（直近20ラウンド）
   - ベスト8スコア一覧

### ハンディキャップの計算方法

**WHS（ワールドハンディキャップシステム）に基づく計算:**

1. 直近20ラウンドを取得
2. ラウンド数に応じてベストスコアを選択：
   - 3-5ラウンド: ベスト1
   - 6-8ラウンド: ベスト2
   - 9-11ラウンド: ベスト3
   - ...（以下同様）
   - 20ラウンド: ベスト8
3. 各スコアの差分を計算：`(スコア - コースレート) × (113 / スロープレーティング)`
4. ベストスコアの平均差分を計算
5. ハンディキャップインデックス = `平均差分 × 0.96`

### グロス/ネット切り替え

1. 「ランキング」タブを開く
2. 上部の切り替えボタンで選択：
   - **ネット（HC適用）**: ハンディキャップを適用したスコアでランキング
   - **グロス（HCなし）**: 実際のスコアのみでランキング

### スコア入力

- スコアとパット数のみ入力します
- ハンディキャップは自動的に計算されます
- スコア保存後、ハンディキャップが自動更新されます

---

## 更新履歴

| 日付 | バージョン | 内容 |
|------|-----------|------|
| 2026-01-09 | v2.2.0 | パーオン率Par3/Par4/Par5別入力、ランキング表示条件変更、正本杯デフォルト |
| 2026-01-08 | v2.1.0 | 2026年度ルール対応、パーオン率ランキング、平均スコア変化ランキング、HC手動更新、アルバトロス |
| 2026-01-08 | v2.0.0 | WHS自動計算機能実装 |
| 2026-01-05 | v1.1.0 | 年度別ファイル管理実装 |
| 2025-12-31 | v1.0.0 | 初回リリース |
